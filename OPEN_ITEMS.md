# 미해결 목록

Kay가 물어보면 이 파일을 읽고 답한다. 해결되면 줄을 지우고 README work log 로 옮긴다.
새 요청이 들어와도 여기 있는 항목은 묻히지 않는다.

## Kay가 줘야 풀리는 것

1. **자격 갱신 비행 코드가 찍힌 로스터 사진** — 특수공항 자격 갱신 비행이 `Activity` 나
   `Remark` 에 어떻게 표시되는지 실물을 못 봤다. 지금 코드는 도착지가 `TAG` 일 때만
   갱신 비행으로 보는데, 이건 데모용으로 박아둔 값이고 `TAG` 는 특수공항 목록에 있지도 않다.
   실제 표기를 보기 전에는 고치지 않는다 — 추측해서 박은 탓에 `SIM1`·`RSV_F`·`S_L+U` 를
   차례로 놓쳤다.

2. **운항편조팀 업무지침 전문** — 운항가이드는 이 지침을 발췌만 싣는다(4.1.2 STAND-BY,
   5.2 휴가 신청, 5.3 스케줄 변경). **RSV 부분 스왑 불가**의 근거가 여기 있을 것으로 보인다 —
   Kay는 실무로 알고 있는데 네 문서(FOM·인사관리 지침·운항가이드·단협) 어디에도 없다.
   운항 RSV 는 `RSV_F` 라는 별도 코드를 쓰는데 STAND-BY 지침에는 SA/SB 만 나온다.

## 객실 Swap Guide 에 있는데 앱에 아직 없는 것

**VAC 양도 불가는 2026-09-22 에 구현했다**(운항·객실 공통). 나머지는 아래.

3. **주 52시간 초과 시 반려** (월~일 기준, HSC·OFC 는 8시간으로 산정) — 근무 시각이
   있으니 바로 계산된다
4. **유닛장 2명 이상 동편조 불가**, 유닛장은 DP 고정
5. **편조에 DP 가능자·방송 가능자 각 1명 필수 배정**
6. **신입승무원·사무장 OJT 비행 DP 스케줄 변경 불가**
7. **C5~7년차 동 편조 매칭 제한**
8. **Swap 을 통한 전 편조 동기 비행 불가**
9. **타사 항공편 Extra SKD Swap 불가** — `AIR`(DH by Air) 는 잡히는데 어느 항공사인지가
   근무표에 없다
11. **AL 입사 13개월 / AR 입사 6개월 경과 후 가능** — `crew-seniority.js` 가 이미 있다
    (사번 앞 4자리가 입사 연월). **다만 어디에도 연결돼 있지 않은 죽은 모듈이다.**
12. **SKD 변경 상호간 휴일 일수 동일** — 양쪽 OFF·VAC 일수를 세면 된다

**4~8 은 편조 명단이 필요한데, 데이터는 이미 들어온다.** 처음에 "근무표에 편조가 없다"고
했던 것은 내가 월간 달력 PDF 만 보고 단정한 오류다. Worker 의 CrewConnex 파서는 로스터
표의 `CC`(편조)·`Pos`(직책) 열을 읽는다. 문제는 그걸 `김제주(PUR), 최크루(FA)` 처럼
**문자열 하나로 뭉쳐 저장**한다는 것이다(`e.crewComposition`). 직책별로 세거나 사번을
보려면 구조를 유지해야 한다.

→ **Kay 확인 필요**: 파서가 읽는 열은 `Date/Pairing/Activity/From/To/C·I/C·O/STD/STA/
A·C/BLH/CC/Pos` 다. 사번·방송등급이 CC 열 안에 같이 오는지, 편조 상세를 따로 열어야
나오는지 확인되면 4~8 중 판정 가능한 것을 구현한다.

## 규정 검토 — 근거를 못 찾은 것

출처 없는 숫자를 규정으로 넣지 않는다. `dutyConsecLimit: 5`(연속 근무일)가 첫 커밋부터
근거 없이 들어가 멀쩡한 스왑을 막고 있었다.

14. **객실 근무교환 마감 시각** — 객실 Swap Guide 는 「영업일 3일」만 정하고 시각이 없는데,
    코드가 기본값 17시를 채우고 있다. 운항은 17시가 명시돼 있다(운항가이드). 객실도 같은지
    Kay 확인 필요.
15. **운항승무원 공휴일·연휴 제한** — 객실은 「OFFMED 만 공휴일·주말 변경 불가」로 확인돼
    그대로 좁혔다. 운항 목록에는 아직 출처 없는 WARN 이 남아 있다.
16. **가족관계 동편조 금지** — 규정은 있는데 앱에 미구현. 넣을지 결정 필요

## 밖에서 답이 와야 하는 것

16. **Apple 심사** — 2026-09-24 **1.2.0 재제출 완료**, `WAITING_FOR_REVIEW`(애플 차례).
    빌드 46, iPad 스크린샷 3-profile 교체, 표시명 「듀티스왑 DutySwap」.
    출시는 `MANUAL` — 승인돼도 Kay가 누를 때까지 나가지 않는다.
    상태는 추론하지 말고 `node scripts/review-status.mjs` 로 조회한다.

    ⚠️ **스플래시 영상이 「SCHEDULE SWAP」이다.** 스토어(듀티스왑 DutySwap)·아이콘
    (듀티스왑)과 합쳐 이름이 셋이라 애플이 이름 불일치로 반려할 수 있다.
    **Kay가 변경 없이 가기로 했다**(2026-09-23). 반려되면 여기부터 본다.

17. **Play Console 조직 계정** — **2026-09-24 등록·결제 완료.**
    계정 이름 `RufnekCrew`, 계정 ID `8365421744866533168`, Google 계정은 배우자
    `hosihana2@gmail.com`. D-U-N-S `696604212` 로 결제 프로필이 자동 연결됐다.

    **계정 소유자 표시가 `hosihana2@gmail.com (강경태)` 로 나온다.** 괄호 안은 그 Google
    계정의 프로필 이름일 뿐이고 구글의 검증 대상이 아니다. 조직의 신원은 D-U-N-S,
    「권한을 부여받은 담당자」(배우자), 본인 확인 신분증(배우자)으로 따로 검증한다.
    **놀라서 Google 계정 이름을 바꾸지 말 것** — 인증 중에 값이 흔들리고, 이름 변경은
    재변경 제한과 Gmail 발신자 이름까지 함께 바꾼다.

    **인증 3개 중 웹사이트는 끝났고 본인 확인 결과를 기다리는 중이다(2026-09-24).**
    - **본인 확인** — 배우자 신분증. **사업자등록증·권한을 부여받은 담당자(배우자) 제출 완료.**
      며칠 걸린다. Kay 신분증으로는 안 된다.
    - ~~**조직 웹사이트 인증**(`rufnekcrew.com`)~~ — **2026-09-24 완료.** Search Console
      도메인 속성 + **DNS TXT**(`google-site-verification=wmwI6Xwo1Mk4hHvjwLKZqKD_kWPqgzlt27QLh-FjEWs`)
      로 확인했다. **이 TXT 를 지우면 인증이 풀린다.** 기존 SPF 레코드와 공존한다.
      DNS 방식이라 **사이트를 어디서 서빙하든 인증은 유지된다** — 호스팅 이전이 막히지 않는다.
      (Claude 의 Cloudflare 토큰에는 DNS 쓰기 권한이 없다 — zone 은 읽기만이라 Kay 가 넣었다.)
    - **전화번호 인증** — 위 둘이 끝나야 열린다.

    등록 때 넣은 값: 조직 전화 `+821086670785`(비공개), 공개 개발자 전화 같은 번호
    (**Google Play 에 공개 표시됨**), 개발자·연락처 이메일 `info@rufnekcrew.com`,
    조직 웹사이트 `https://rufnekcrew.com`, 수익 창출 「예」, 카테고리 「해당 사항 없음」.

    ⚠️ 구글이 대조하는 상대는 사업자등록증이 아니라 **D-U-N-S 로 조회되는 D&B 프로필**이다.
    등록 때만 보는 것이 아니라 Google 결제 프로필의 법적 이름·주소를 **계속 대조한다.**
    어긋나면 배너가 뜨고 기한 내 해결하지 못하면 개발자 계정이 제한되어 **Play 의 모든
    앱이 삭제된다**. D&B 법적 이름은 한글 **「러프넥크루」** 다(구글이 자동으로 채운 값).
    영문 `RufnekCrew` 는 **개발자 표시명**일 뿐이니 "통일"하겠다고 건드리지 말 것.

18. ~~**API 도메인 전환**~~ — **2026-09-22 완료.** `API_BASE` 는
    `https://api.rufnekcrew.com` 이고 워커에 커스텀 도메인이 붙어 있다.
    `worker/wrangler.toml` 의 `workers_dev = true` 를 절대 빼지 않는다. 한 번 뺐다가
    설치된 앱 전원이 약 2분간 서버에서 끊겼다.

19. **2027년 7월** — 배우자 명의 계정 이전(README 「계정 전환 · 유료화 재개 절차」)

## 다음에 바로 할 것 (2026-09-23 저녁 기준)

22. ~~**Release 빌드 46 업로드 → 1.2.0 재제출**~~ — **2026-09-24 완료.**
    어제 「스크린샷 완료」라고 적었던 것은 파일만 완료였고 App Store Connect 에는
    옛 08 번(「CrewSwap PRO」)이 올라가 있었다. 교체하고 순서를 3 번 자리로 되돌렸다
    (새 스샷은 세트 맨 뒤에 붙는다 — 업로드 후 반드시 순서를 확인할 것).
23. ~~**안드로이드 AAB 가 2026-09-09 자다**~~ — **2026-09-24 재빌드 완료.** 26번 참조.
24. **`splash.mp4` 가 「SCHEDULE SWAP」** — 지금은 그대로 가기로 했다(16번 참조).
    나중에 바꾼다면 영상 자산을 새로 만들어야 한다.
25. ~~**rufnekcrew-site 를 내 손으로 배포할 수 없다**~~ — **2026-09-24 해결.**
    ChatGPT 호스팅(`rufnekcrew.arpw-56.chatgpt.site`)에서 **Kay 의 Cloudflare 로 옮겼다**:
    `https://rufnekcrew-site.tae26001.workers.dev`. 빌드가 이미 Worker 형태로 나오고
    (`.wrangler/deploy/config.json` 이 `dist/server/wrangler.json` 을 가리킨다) d1·r2
    바인딩이 없어 그대로 올라갔다. **프로젝트 루트에서** `npx wrangler deploy` 로 배포한다
    (`dist/server` 안에서 하면 설정 파일이 둘이라며 거부한다).

    ⏳ **남은 것: `rufnekcrew.com` DNS 전환.** 지금도 루트 A 레코드가 옛 호스팅
    (`162.159.143.30`)을 가리켜 **공개 주소에는 아직 「CrewSwap」이 떠 있다.**
    Claude 토큰에 dns 쓰기 권한이 없어 Kay 가 대시보드에서 해야 한다.
    Play 웹사이트 인증은 DNS **TXT** 로 걸려 있어 A 레코드를 바꿔도 풀리지 않는다.

26. **안드로이드 AAB 재빌드 완료(2026-09-24)** — 8.4MB, 업로드 키 서명 확인.
    번들 안까지 열어 확인했다: manifest·홈 화면 이름 「듀티스왑」, API 는 운영 도메인,
    옛 이름 0건, versionCode 4 / 1.2.0.
    ⚠️ **빌드에 `JAVA_HOME` 지정이 필요하다** — 시스템에 JDK 가 없어 `./gradlew` 가 바로
    실패한다. Android Studio 번들 JDK 를 쓴다:
    `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew bundleRelease`

27. **Play 서류 승인 대기 중** — 홈 카드에서 본인 확인이 사라졌지만 그것은 **접수**이지
    승인이 아니다. 전화번호 인증 화면은 여전히 「신원을 인증하고 Google 에서 서류를
    승인받는 등의 작업」을 먼저 끝내라고 한다. 결과는 `hosihana2@gmail.com` 메일로 온다.
    승인되면 전화번호 2개(연락처·개발자, 둘 다 `+821086670785`)를 각각 인증하면
    「앱 만들기」가 열린다.

