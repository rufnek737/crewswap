# 미해결 목록

Kay가 물어보면 이 파일을 읽고 답한다. 해결되면 줄을 지우고 README work log 로 옮긴다.

## Kay가 줘야 풀리는 것

1. **자격 갱신 비행 코드가 찍힌 로스터 사진** — 특수공항 자격 갱신 비행이 `Activity` 나
   `Remark` 에 어떻게 표시되는지 실물을 못 봤다. 지금 코드는 도착지가 `TAG` 일 때만
   갱신 비행으로 보는데(`app.js`), 이건 내가 데모용으로 박아둔 값이고 `TAG` 는 특수공항
   목록에 있지도 않다. 실제 표기를 보기 전에는 고치지 않는다 — 추측해서 박은 탓에
   `SIM1`·`RSV_F`·`S_L+U` 를 차례로 놓쳤다.

## 규정 검토 — 근거를 못 찾은 것

화면에 규정 15개가 잡혀 있다. 하나씩 같이 훑는 중이고, 아래는 아직 출처를 못 댄 것들이다.
출처 없는 숫자를 규정으로 넣지 않는다 — `dutyConsecLimit: 5`(연속 근무일)가 첫 커밋부터
근거 없이 들어가 멀쩡한 스왑을 막고 있었다.

2. **편조 등급 조합표** (A→A/B/C, B→A/B, C→A) — 출처 미확인
3. **공휴일·연휴 스왑 제한** — 근거 미확인
4. **신청 마감 2영업일 17시** — 근거 미확인
5. **RSV 근무시간 산정 기준** — 대기를 몇 시간으로 세는지 확인 필요
6. **가족관계 동편조 금지** — 규정은 있는데 앱에 미구현. 넣을지 결정 필요

## 밖에서 답이 와야 하는 것

7. **Apple 심사** — 2.1(b) 답장을 2026-09-17 에 보냈고 회신이 없다. 화요일까지 무응답이면
   재촉 메시지를 보낸다(문구 준비됨).
8. **D-U-N-S** — Case 34850994, 2026-09-28~10-07 결과 예상. 반려되면 개인 계정 +
   12명×14일 테스트로 선회할지 검토한다.

## 심사가 끝나면 할 것

9. **API 도메인 전환** — `API_BASE` 를 `api.rufnekcrew.com` 으로 바꾼다.
   `worker/wrangler.toml` 의 `workers_dev = true` 를 절대 빼지 않는다. 한 번 뺐다가
   설치된 앱 전원이 약 2분간 서버에서 끊겼다.
10. **2027년 7월** — 배우자 명의 계정 이전(README 「계정 전환 · 유료화 재개 절차」)
