node_modules/
.netlify/
*.bak
.DS_Store
Thumbs.db
.env
.env.local
.claude/settings.local.json
.vscode/
.idea/
www/

# 빌드/캐시 아티팩트
worker/.wrangler/
ios/App/App.xcarchive/
ios/App/archive.plist

# KV→D1 이관 산출물 — 비밀번호 해시·실명·사번·연락처가 들어있어 공개 저장소에 올리지 않는다.
backup/
worker/migrate-data.sql

# App Store Connect API 키 — 실수로 커밋되지 않도록 차단 (실제 키는 ~/.appstoreconnect에 보관)
*.p8
build/

# Android 업로드 서명 — 비밀번호가 들어 있어 저장소에 넣지 않는다.
# 원본은 ~/crewswap-upload.jks 와 ~/crewswap-upload-keystore.txt
android/keystore.properties
